language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
services:
  - redis
install:
- "pip install redis"
- "if [[ $TRAVIS_PYTHON_VERSION == 2.7 ]]; then pip install mock; fi"
script: "python runtests.py"
notifications:
  email: true
